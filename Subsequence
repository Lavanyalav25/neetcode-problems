class Solution {
    public boolean isSubsequence(String s, String t) {
        int ind=-1;
        for(int i=0;i<s.length();i++){
            ind=t.indexOf(s.charAt(i),ind+1);
            if(ind==-1){
                return false;
            }
        }
        return true;
    }
}
